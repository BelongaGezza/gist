# PLATFORM_VERIFICATION.md

What has actually been verified, on which machine, when. Update the row whenever you build,
test or change something. "Not verified" is a valid and honest state.

| Area | macOS (M1 Pro) | Windows (ProArt13) | Linux | Notes |
|---|---|---|---|---|
| Rust core: `cargo test/clippy/fmt` | verified 2026-09-12 (per CLAUDE.md history) | **verified 2026-09-20** (117 tests pass; clippy -D warnings and fmt clean) | CI only | Pinned Rust 1.88.0 |
| Rust core: `cargo build -p gist-ffi` | Apple release build verified by CI (`apple-build`, PR #31) | **verified 2026-09-20** (debug and release, x64 MSVC; static CRT; release DLL has no VC++ runtime imports; C# spike 20/20 on the release DLL) | CI (`core-test` ubuntu, and now windows-latest) | cdylib is what the Windows shell loads; release keeps `panic = "unwind"` |
| Dependency audit: `cargo audit` (root + `fuzz/Cargo.lock`) | n/a | **verified 2026-09-20** (both clean; fuzz `cargo check` passes) | CI (`core-quality`) | RustSec DB, 1251 advisories |
| Fuzz targets (`cargo-fuzz`, nightly) | ran locally 2026-09-18 (~100 s per target, no crashes) | not run | **verified 2026-09-20** on GitHub Actions ubuntu (manual run, 20 s per target, all 4 pass, no crashes; nightly 120 s run not yet observed green) | Needs `RUSTUP_TOOLCHAIN: nightly` (fixed in #26) |
| SwiftUI app: `xcodebuild build/test` | verified 2026-09-19 (49 tests, plus GitHub CI) | n/a | n/a | Apple only |
| WinUI 3 shell (`apps/windows`) | n/a | **hello spike builds and runs 2026-09-20** (unpackaged, x64, Windows App SDK 2.5.1, `gist_ffi.dll` loads); MSIX built in a temp copy only (not installed/run: Developer Mode off); VS has only the C++ workload (not needed to build); real shell not started | n/a | ADR-017/018; plan `docs/windows-development-plan.md` |
| Windows key custody (DPAPI) | n/a | **verified 2026-09-20**: 13 xunit tests pass on real DPAPI (create-once, 32-thread race, tamper/truncate, entropy mismatch). MSIX/roaming/master-key-loss not verified | n/a | ADR-016 |
| FFI bindings (uniffi) | Swift generated | **C# spike verified 2026-09-20** (20/20 checks, net10.0 x64 debug). Pinned generator security-reviewed (acceptable with conditions, `docs/security-review-windows-bindgen.md`). Release/ARM64/MSIX not verified | n/a | ADR-015 |
| Windows solution (`apps/windows`, W1) | n/a | **verified 2026-09-20/21**: GIST.sln (GIST.Core, GIST.Core.Tests, GIST.App) builds with 0 warnings under warnings-as-errors; 118 xunit tests pass (CoreClient, key custody, storage paths, filtering, native resolver) against a real `GistCore`. Exact NuGet pins + lock files. **Also verified in CI 2026-09-21** (`windows-build`: .NET SDK 10.0.400 + Windows SDK 10.0.26100 on `windows-latest`, bindings reproducible, Release build, 118 tests, NuGet vulnerability gate clean, ARM64 DLL cross-compiled: builds, not run) | not run | Central package management, NuGet audit at High |
| Windows core client (`CoreClient`) | n/a | **verified 2026-09-21**: key acquired eagerly before `NewWithReadKey`; corrupt key => blocking state, no regeneration; DRM by type; read-after-encrypt. App-layout consumer reaches `Ready` (found and fixed a DllNotFound in that layout via an absolute-path resolver) | not run | Unverified: packaged (MSIX) layout, ARM64 |
| Dependency bumps 2026-09-20 (uniffi 0.32.1, zip 8.6.0, sha2 0.11, aes-gcm 0.11.1) | **not verified** (see PENDING_APPLE_CHANGES.md) | verified 2026-09-20: `cargo test --workspace`, C# generator + 20-check spike; aes-gcm 0.10 known-answer test passes under 0.11 | CI (ubuntu/macos core tests green) | Swift bindings not regenerated; `apple-build` not run on these |
| Windows app wiring (`GIST.App` + `CoreClient`) | n/a | **verified 2026-09-21** (scripted, `apps/windows/GIST.App/verify-library.ps1`): seeded item visible via UI Automation, `state=Ready items=1`; garbage key file => blocking page, no delete/regenerate action, key bytes unchanged. Unverified: screenshot/visual, Retry click, other error pages, MSIX, ARM64 runtime | not run | Uses `GIST_DATA_ROOT` scratch profile, never the real one |
| rusqlite 0.31 -> 0.40.2 (bundled SQLite 3.45.0 -> 3.53.2) | **not verified** (see PENDING_APPLE_CHANGES.md) | **verified 2026-09-21**: `cargo test --workspace` (125 tests incl. 7 new old-database compatibility tests), `clippy -D warnings`, `fmt --check`, `cargo deny check bans licenses sources advisories` all clean; release `gist_ffi.dll` still has no dynamic VC++ runtime imports; release panic containment still ok | CI (`core-test` ubuntu green on #48) | Zero source changes needed. Forward *and* backward on-disk compatibility proven locally (a 3.53.2-written library reopens, searches and accepts writes under 3.45.0). Swift bindings not regenerated; xcframework not rebuilt |
| Windows Library screen (`GIST.App`, W2) | n/a | **verified 2026-09-22**: 23 FlaUI click-through tests (5 consecutive runs, unlocked interactive desktop): enablement, Ctrl+F/Esc/Delete/Ctrl+A/Enter/Ctrl+O, all five sorts, prefix search, tag editor + filter, complete-delete Remove with the user's original SHA-256 unchanged, Encrypt warning/result/lock marker, URL dialog rejects non-https with no network, new collection, native picker open/cancel/select-by-path, locked-file removal warning + close button + next-start sweep. **Unverified:** visual look (screenshots render black in this session), theme/Mica/Narrator/high contrast, right-click menu, double-click, narrow-width overflow, real https import, DRM dialog in the UI, MSIX, ARM64 runtime. UI tests are not run in CI. | not run | Manual checklist: `docs/qa-manual-clickthrough-windows.md` |
| Removal semantics (`gist-core`/`gist-store`, all platforms) | **not verified on a real library** (Apple CI compiled it and passed 48 Swift tests on temp dirs; see `PENDING_APPLE_CHANGES.md`) | **verified 2026-09-22**: complete delete, shared-copy reference check in the same transaction, containment check (only NotFound counts as nothing-to-delete), long-path SQLite open, sweep case-insensitivity, locked-file reporting; user originals asserted byte-identical by SHA-256 | CI (ubuntu/macos core tests, `apple-build`) | Destructive path: macOS hand-test on a backed-up library recommended before adopting the sweep |
| Manual UI click-through | outstanding (`docs/qa-manual-clickthrough-m2.md`) | n/a | n/a | Needs a person |

Convention: `verified <date>` means the check was executed on that platform. Anything a session
could not run gets a `// TODO(test): verify on [PLATFORM: X]` in code and `not verified` here.
