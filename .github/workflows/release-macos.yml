name: release-macos
on:
  push:
    tags: ["v*"]

permissions:
  contents: read
  # NOTE: this pipeline is currently a stub (see the guard step below). Once
  # M4 implements real release publishing, revisit this block — creating a
  # GitHub release / uploading assets needs `contents: write`, scoped to
  # just that job step, not added speculatively now.

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      # Build universal xcframework, sign, notarise, produce DMG
      # Requires: APPLE_DEVELOPER_ID_CERT, APP_STORE_CONNECT_API_KEY in repo secrets
      # See tools/build-core-xcframework.sh and tools/notarize.sh
      - name: Guard — release pipeline not yet implemented
        run: |
          echo "ERROR: release-macos pipeline is a stub. Implement fully in M4 before tagging a release." >&2
          exit 1
