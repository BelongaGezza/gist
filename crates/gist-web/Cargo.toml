[package]
name = "gist-web"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"

[dependencies]
ureq      = { version = "2", default-features = false, features = ["rustls"] }
scraper   = "0.20"
url       = "2"
gist-model = { path = "../gist-model" }
thiserror = { workspace = true }
