[package]
name = "gist-imageprep"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"

[dependencies]
gist-model = { path = "../gist-model" }
gist-core  = { path = "../gist-core" }
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
