# assets

Source assets for app icons and other design deliverables. See [`docs/iconspecification.md`](../docs/iconspecification.md) for the design spec these assets should follow.

Currently empty — populate with the exported icon source files (per-platform `.icns`/`.ico`/`.svg`/vector sources) as they're produced.
