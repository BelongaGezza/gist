namespace Gist.Core.Tests;

// Placeholder so `dotnet test` has something to run before real tests land (W1). Delete once
// GIST.Core.Tests has real coverage.
public class SolutionSmokeTests
{
    [Fact]
    public void Solution_builds_and_runs_tests() => Assert.True(true);
}
